# Optional. Without it Thirdplace runs in "offline agent" mode: every agent task
# falls back to a deterministic template so the app stays fully usable.
ANTHROPIC_API_KEY=sk-ant-...

# Optional overrides
THIRDPLACE_MODEL=claude-opus-5
PORT=3000
THIRDPLACE_DB=./data/thirdplace.json
